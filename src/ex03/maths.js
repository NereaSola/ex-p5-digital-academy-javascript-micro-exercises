/**
 * Compares three numbers and returns the largest one.
 * @param {number} a 
 * @param {number} b 
 * @param {number} c 
 * @returns {number} The maximum value
 */
export function findLargestOfThree(a, b, c) {
    return Math.max(a, b, c);
}